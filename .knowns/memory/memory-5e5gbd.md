---
id: 5e5gbd
title: Project workflow decision
layer: project
category: decision
status: proposed
tags:
  - project
  - workflow
createdAt: '2026-07-10T09:25:41.891Z'
updatedAt: '2026-07-10T09:25:41.891Z'
---

<task-notification> <task-id>a4e2ce1ba55f92e75</task-id> <tool-use-id>toolu_01QCjSumTo76LSojxugo5T3d</tool-use-id> <output-file>/private/tmp/claude-501/-Users-scroobz-Navigation/26e7e100-aee4-46bd-b54c-d9e9a8502222/tasks/a4e2ce1ba55f92e75.output</output-file> <status>completed</status> <summary>Agent "Explore forum feature and PocketBase schema" finished</summary> <note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note> <result>Now I have complete findings. Here is the full report. ## 1. Forum API routes — ID typing and field-name mismatches **`/Users/scroobz/Navigation/saily/web/src/app/api/forum/posts/route.ts`** - `ThreadMeta.id: number`, `continue_thread_id: number | null` (lines 6-11) - `getThreadMeta(pocketbase, threadId: number)` queries `.from("forum_threads").select("id,kind,puzzle_date,continue_thread_id").eq("id", threadId)` (13-18) — real PocketBase ids are strings, `.eq("id", threadId)` will quote a NaN/number. - `GET`: `const threadId = Number(url.searchParams.get("threadId"))` then `if (!Number.isFinite(threadId) || threadId &lt;= 0)` → 400 (line 29-33). Any real PB id (e.g. `abc123xyz`) makes `Number()` return `NaN`, guaranteed 400. - Posts query: `.from("forum_posts").select("id,thread_id,parent_post_id,user_id,body,result_payload,created_at,updated_at,profiles!forum_posts_user_id_fkey(username)").eq("thread_id", threadId)` (54-57) — queries column `thread_id`, but the PB schema field is named `thread` (a RelationField), and the `profiles!forum_posts_user_id_fkey(...)` embed syntax isn't supported by `real-query.ts` at all (see #5). - Votes/reactions queried via `.eq("post_id", postIds)`/`.in("post_id", postIds)` (76-77) — PB schema field is `post`, not `post_id`. - `voteCountByPost`/`reactionCountByPost` are typed `Map&lt;number, ...&gt;` (84, 94) keyed by `post.id`, `row.post_id` — also number-typed. - `POST`: payload typed `{ threadId?: number; parentPostId?: number | null; body?: string; ... }` (136-141), `Number(payload.threadId)`, same `Number.isFinite` guard (143-149). Insert uses `{ thread_id: threadId, parent_post_id: parentPostId, user_id: user.id, ... }` (187-193) — again `thread_id`/`parent_post_id` column names that don't exist on `forum_posts` (real field is `thread`; `parent_post_id` legacy text field actually does exist per migration, see #2, but `thread_id` does not). **`/Users/scroobz/Navigation/saily/web/src/app/api/forum/vote/route.ts`** - `PostThreadMeta.post_id: number; thread_id: number` (6-12). - `getPostThreadMeta`: `.from("forum_posts").select("id,thread_id,forum_threads!inner(id,kind,puzzle_date,continue_thread_id)").eq("id", postId)` (15-19) — same `thread_id` field mismatch, and `forum_threads!inner(...)` embed syntax unsupported by `real-query.ts`. - `POST`: `const postId = Number(payload.postId)`, `!Number.isFinite(postId) || postId &lt;= 0` → 400 (48-53). - Vote existence check/insert/delete/count all use `.eq("post_id", postId)` (76-97) — PB field is `post`. **`/Users/scroobz/Navigation/saily/web/src/app/api/forum/reaction/route.ts`** - Structurally identical to vote route: `PostThreadMeta` typed as `number`, `Number(payload.postId)` + `Number.isFinite` guard (48-54), and all `forum_post_reactions` queries use `.eq("post_id", postId)` (77-108) instead of `post`. **`/Users/scroobz/Navigation/saily/web/src/app/api/forum/threads/route.ts`** - `ThreadRow.id: number`, `continue_thread_id: number | null` (7-18). - Doesn't call `Number()` on an incoming id directly (it takes a `date` string), but it calls `pocketbase.rpc("ensure_forum_threads", { p_puzzle_date: date })` (33-35), which is the unimplemented RPC (see #3/#4) that always returns `{ data: null, error: null }`, so `rows` is always `[]` and `defaultThreadId` always `null` when `access.allowed` — this route currently can never surface real threads. - Has self-documenting comments (13-17, 41-49) already flagging the `hidden_until_completion` column gap and the RPC stub. ## 2. PocketBase schema (backend/migrations) `/Users/scroobz/Navigation/saily/backend/migrations/2_supabase_replacement_collections.go`, function `createForum` (lines 189-253): - **`forum_threads`**: text `puzzle_date` (required), select `kind` (`daily_live`/`ongoing`), text `title`, text `continue_thread_id` (optional, plain text not relation), unique index on `(puzzle_date, kind)`. Note: `hidden_until_completion` (BoolField) added later by migration 9. - **`forum_posts`**: `thread` — **RelationField** (required, `CollectionId: threads.Id`, cascade delete) — NOT `thread_id`. Plus text `user_id` (required), text `parent_post_id` (optional, plain text — not a relation), text `body` (required), JSON `result_payload` (optional). Indexes on `thread` and `parent_post_id`. - **`forum_post_votes`**: `post` — **RelationField** (required, `CollectionId: posts.Id`, cascade delete) — NOT `post_id`. Plus text `user_id`. Unique index on `(post, user_id)`. - **`forum_post_reactions`**: `post` — **RelationField** (required → posts), text `user_id`, text `emoji` (required). Unique index on `(post, user_id, emoji)`. So bug #2 from the ticket is precisely confirmed: routes query `thread_id`/`post_id` text columns that don't exist; the real relation fields are named `thread` and `post`. ## 3. `9_forum_thread_gate.go` — ensure_forum_threads gap `/Users/scroobz/Navigation/saily/backend/migrations/9_forum_thread_gate.go`: - Adds `daily_editions.forum_thread_id` (nullable relation → `forum_threads`) and `forum_threads.hidden_until_completion` (BoolField, backfilled to `true` for all existing rows on migration-up). - Header comment (8-16) cites the launch seed bible section "6. Forum/Thread Gating" and the original ticket `za7hd0-implement-saily-forum-thread-gate-seed-relationship.md`. - Key comment block (49-54): `core.BoolField` has no schema-level default, so `true` is only a one-time backfill; **"Any future thread-creation code path (e.g. a real implementation of the currently-stubbed `ensure_forum_threads` flow) must explicitly set `hidden_until_completion: true` when inserting a new row."** — this is the explicit self-documented gap: the migration adds the schema, but no code path actually creates `forum_threads` rows with this flag set, because `ensure_forum_threads` (the thing that's supposed to create/find the day's threads) is unimplemented. ## 4. Frontend caller of `ensure_forum_threads` Only one call site: `/Users/scroobz/Navigation/saily/web/src/app/api/forum/threads/route.ts` line 33: ```ts const { data, error } = await pocketbase.rpc("ensure_forum_threads", { p_puzzle_date: date }); ``` Expected behavior (per Supabase-era naming and the surrounding comments/types): given a `puzzle_date`, find-or-create the day's `forum_threads` rows (one `daily_live` + one `ongoing`, per the unique index on `(puzzle_date, kind)`), returning rows shaped like `ThreadRow` (`id, kind, puzzle_date, title, continue_thread_id, hidden_until_completion`). It's the thing that should populate "today's" threads on first access of a given date, seeding `hidden_until_completion: true`. Actual behavior: `/Users/scroobz/Navigation/saily/web/src/lib/pocketbase/server.ts` line 11 lists `ensure_forum_threads` in `UNIMPLEMENTED_RPCS`, and the generic `rpc()` implementation (lines 40-47) just logs a warning and resolves `{ data: null, error: null }` for any name in that set — no PocketBase call is made at all. This means `/api/forum/threads` always returns an empty `threads: []` and `defaultThreadId: null` (when access is allowed), i.e. no way to actually open a thread today. This is arguably a fourth, more fundamental bug than the three in the ticket description — threads are never created, so posts/votes/reactions have nothing valid to attach to even once the string/relation bugs are fixed. ## 5. `real-query.ts` shape/API `/Users/scroobz/Navigation/saily/web/src/lib/pocketbase/real-query.ts` — a hand-rolled Supabase-shaped query builder (`RealPocketBaseQuery`) translating `.from(...).select(...).eq(...)` calls into real PocketBase REST calls, authenticated as the PB superuser (per-row authz is left to the calling route, not PB rules — see header comment 6-16). - `.from(collection)` → `new RealPocketBaseQuery(collection)`. - `.select(fields, { count, head })` — parses top-level comma-separated field list; supports embedded-relation syntax `profiles(username)` **only** if a matching entry exists in `JOIN_CONFIG` (line 25-27), currently only `"comments.profiles": { localField: "user_id", targetCollection: "profiles", targetField: "shared_user_id" }`. **There is no `forum_posts.profiles` (or `forum_posts.forum_threads`) entry** — so both `posts/route.ts`'s `profiles!forum_posts_user_id_fkey(username)` embed and `vote/route.ts`/`reaction/route.ts`'s `forum_threads!inner(...)` embed will throw `Unsupported embedded select ... not yet configured in JOIN_CONFIG` at runtime, a 4th latent bug beyond the three named. Also note the `!fkey`/`!inner` PostgREST modifier syntax isn't parsed at all — `parseSelect`'s regex `^([a-zA-Z0-9_]+)(!\w+)?\(` does tolerate the `!xxx` suffix but then looks up `JOIN_CONFIG[collection.embedName]` using only the base name, so even a correctly configured join key wouldn't include the modifier. - `.insert(rows)`, `.upsert(rows, { onConflict })`, `.update(data)`, `.delete()` — set `op` and defer to `execute()`. - Filter builders: `.eq`, `.neq`, `.gt`, `.gte`, `.lt`, `.lte`, `.is`, `.not`, `.contains`, `.ilike`, `.in` — all push a PocketBase filter-string fragment into `this.filters`, joined with `&amp;&amp;` in `filterExpr()`. **`.or()` and `.range()` both `throw new Error(... not implemented yet)`** (lines 248-250, 272-274) — any forum route needing OR-filters or range-based pagination will hard-fail. - `.order(col, { ascending })`, `.limit(n)`. - `.single()` / `.maybeSingle()` — execute then unwrap first row (or error if none for `.single()`). - Thenable: the class implements `.then()` so `await pocketbase.from(...).select(...)...` works directly without an explicit `.execute()` call. - Under the hood, `select` hits `GET /api/collections/{collection}/records?filter=...&amp;sort=...&amp;perPage=...` via `pbFetch` using the PB superuser token; `insert`/`update`/`delete` hit the corresponding REST endpoints per matched row. `count`/`head` mode does a `perPage=1` request and returns `totalItems` without rows. - Values are quoted via `quoteValue()` — numbers/booleans unquoted, everything else (including a string PB id) wrapped in single quotes and escaped. This is filter-string generation for PocketBase's `filter` query param, so **string IDs work fine here as long as callers stop coercing them to `Number()` first**. Implication for the fix: forum routes need to (a) stop calling `Number()`/`Number.isFinite` on thread/post ids and just treat them as opaque strings, (b) query/insert using `thread`/`post` as the field names (not `thread_id`/`post_id`), and (c) either add `forum_posts.profiles` / `forum_posts.forum_threads` entries to `JOIN_CONFIG` or avoid the embedded-select syntax and instead do a follow-up lookup (similar to how `posts/route.ts` already manually joins votes/reactions via separate `.in()` queries) since `real-query.ts` explicitly says "Extend this as more routes move off the stub." ## 6. `discuss-forum.tsx` structure and ID typing `/Users/scroobz/Navigation/saily/web/src/components/discuss-forum.tsx` (975 lines), exported component `DiscussForum({ initialDate, isAuthenticated })`. - **Types are number-typed throughout**, matching the API's (currently broken) number contract: - `Thread.id: number`, `continue_thread_id: number | null` (9-16) - `Post.id: number`, `thread_id: number`, `parent_post_id: number | null` (27-41) - State: `selectedThreadId: useState&lt;number | null&gt;` (180), `replyBodyByPost: Record&lt;number, string&gt;`, `replyOpenByPost: Record&lt;number, boolean&gt;` (191-192) - Functions: `loadPosts(threadId: number)` (248), `createPost(parentPostId: number | null)` (278), `toggleVote(postId: number)` (389), `toggleReaction(postId: number, emoji: string)` (412) - `buildTree(posts: Post[])` builds a `Map&lt;number, PostNode&gt;` keyed by `post.id` (75-87) - **Data flow**: `loadThreads(date)` fetches `/api/forum/threads?date=...` → sets `threads`/`selectedThreadId`/`dayAccess`. `loadPosts(threadId)` fetches `/api/forum/posts?threadId=${threadId}` (interpolated directly into the querystring — works fine with a string id) → sets `posts`. `createPost` POSTs to `/api/forum/posts` with `{ threadId: selectedThreadId, parentPostId, body, resultPayload }`. `toggleVote`/`toggleReaction` POST to `/api/forum/vote` / `/api/forum/reaction` with `{ postId }` / `{ postId, emoji }`. - **Author rendering**: `displayName(post)` (70-73) reads `post.profiles` (array-or-object shape) and returns `profile?.username ?? "player"`. It's rendered inside `renderNode(node, depth)` at line 507-509: ```tsx &lt;span style={{ ...dataText, fontSize: "0.75rem", color: "var(--primary)", letterSpacing: "0.04em" }}&gt; {displayName(node)} &lt;/span&gt; ``` This is the **only** place author identity is shown — just a text username, right next to the timestamp (510-512), inside the post header `&lt;div className="flex justify-between items-center mb-2"&gt;` (506). **No `&lt;Image&gt;`/avatar element exists anywhere in this file** — confirmed via full read, and via `grep -n "avatar\|Avatar" discuss-forum.tsx` returning nothing (implicitly, since `avatar.ts` symbol import doesn't appear in the file at all). This is exactly the STS-145 gap: `renderNode`'s header div (line 506-513) is where a `getRobotAvatarDataUri(node.user_id, ...)` `&lt;Image&gt;` would need to be added, using `node.user_id` (which the `Post` type already carries, line 31) as the DiceBear seed. - Composer/reply forms don't render avatars either (own-user avatar isn't shown for the composer, only for existing posts per the AC). ## 7. `lib/avatar.ts` signature and usage pattern `/Users/scroobz/Navigation/saily/web/src/lib/avatar.ts`: ```ts export type AvatarExpression = "neutral" | "happy" | "sad" | "surprised" | "serious"; export function getRobotAvatarDataUri(seed: string, size = 64, expression: AvatarExpression = "neutral") { return createAvatar(bottts, { seed, size, radius: 50, backgroundColor: [...], mouth: MOUTH_MAP[expression] }).toDataUri(); } ``` Uses `@dicebear/core` + `@dicebear/bottts`, returns a data-URI string suitable directly as an `&lt;Image src&gt;`. Example usage in `/Users/scroobz/Navigation/saily/web/src/components/auth-status.tsx`: - Import: `import { getRobotAvatarDataUri } from "@/lib/avatar";` (line 7) - Called with the user's id as seed and a pixel size, no expression override: `avatarUrl: getRobotAvatarDataUri(session.user.id, 48)` (line 50) and `getRobotAvatarDataUri(user.id, 48)` (line 71) — stored in local state field `avatarUrl: string` (line 13). - Rendered: `&lt;Image className="profile-avatar" src={auth.avatarUrl} alt="Profile avatar" width={24} height={24} unoptimized /&gt;` (line 123) — note `unoptimized` is required since it's a data URI, and `width`/`height` are independent of the seed-size argument passed to the generator. Other usage sites confirming the pattern (seed = user/character id, size varies by placement): `profile-page.tsx:63` (`getRobotAvatarDataUri(user.id, 96)`), `home-page.tsx:70,74` (`getRobotAvatarDataUri(todayCharacter.avatarSeed, 64)` / `40`), `mission-briefing.tsx:18`, `mission-complete.tsx:46`, `narrative-update.tsx:16` (these three use `character.avatarSeed` + an `expression` argument, not applicable to forum posts). For forum posts, the STS-145 fix would follow the `auth-status.tsx` pattern exactly: `getRobotAvatarDataUri(post.user_id, &lt;small size like 32-40&gt;)` rendered via `&lt;Image ... unoptimized /&gt;` in `renderNode`'s header (discuss-forum.tsx line ~506-513).</result> <usage><subagent_tokens>74167</subagent_tokens><tool_uses>19</tool_uses><duration_ms>98848</duration_ms></usage> </task-notification>.
