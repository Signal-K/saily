---
id: 1v6auk
title: Project workflow decision
layer: project
category: decision
status: proposed
tags:
  - project
  - workflow
createdAt: '2026-07-10T09:26:15.784Z'
updatedAt: '2026-07-10T09:26:15.784Z'
---

<task-notification> <task-id>a4b16493e27b27a15</task-id> <tool-use-id>toolu_01JAjeoQGiXYPgfuwV3nL6yj</tool-use-id> <output-file>/private/tmp/claude-501/-Users-scroobz-Navigation/26e7e100-aee4-46bd-b54c-d9e9a8502222/tasks/a4b16493e27b27a15.output</output-file> <status>completed</status> <summary>Agent "Explore games routes and site nav" finished</summary> <note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note> <result>I now have everything needed for a thorough report. ## Report ### 1. Main site nav/header (`DailyTransitMasthead`) File: `/Users/scroobz/Navigation/saily/web/src/components/daily-transit-masthead.tsx` (client component, imported and rendered in the root layout). Rendering order in `/Users/scroobz/Navigation/saily/web/src/app/layout.tsx` (lines 432‑441): ``` &lt;PostHogRuntime /&gt; &lt;SWRegister /&gt; &lt;DailyTransitMasthead initialTheme={initialTheme} /&gt; &lt;EcosystemBar /&gt; &lt;main className="dt-page-shell"&gt; &lt;BreadcrumbsNav /&gt; {children} &lt;/main&gt; &lt;Analytics /&gt; ``` `DailyTransitMasthead` structure (lines 264‑319): - `&lt;header className="dt-masthead"&gt;` — `position: sticky; top: 0; z-index: 70` (lines 30‑37), bottom border `3px double`. - `.dt-masthead-top` — grid row: brand | primary nav | actions (lines 39‑48, 264‑299). - Brand link (`/`, logo + "The Daily *Transit*" + "Citizen Science Daily" subtitle). - `&lt;nav className="dt-primary-nav"&gt;` — desktop links from `NAV_LINKS` (lines 13‑21): Stories `/articles`, Missions `/games/today`, Archive `/calendar`, Consensus `/discuss`, Stats `/leaderboard`, Registry `/search`, About `/about`. No `/games` overview link exists yet. - `.dt-actions` — ThemeToggle + mobile menu toggle. - Below `.dt-masthead-top`, a `&lt;nav id="dt-mobile-nav"&gt;` (collapsible mobile menu) using the same `NAV_LINKS`. - Immediately below the masthead (sibling in layout.tsx, not inside `&lt;header&gt;`), `EcosystemBar` (`/Users/scroobz/Navigation/saily/web/src/components/ecosystem-bar.tsx`) renders a thin `.dt-ecosystem-bar` strip with external ecosystem links (Landnam, Star Sailors, GitHub) + Imprint, `border-bottom: 1px solid`. **Where a ticker would slot in**: There is no ticker today. The natural insertion point is a new bar between `.dt-masthead-top` and the closing `&lt;/header&gt;` (i.e., inside `DailyTransitMasthead`, above/below the mobile nav), or as its own top-layer component placed between `DailyTransitMasthead` and `EcosystemBar` in `layout.tsx` (both are already stacked as separate sticky/non-sticky bars, so adding a third bar follows existing conventions). Note `z-index: 70` is already claimed by the masthead — a ticker either needs to live inside that sticky header (sharing z-index) or get its own zindex if it should also stick. There's also an existing **`DiscoveryHeadline`** component (`/Users/scroobz/Navigation/saily/web/src/components/discovery-headline.tsx`) that is very close in spirit to a "breaking news ticker" — it's a client component that polls the `discoveries` PocketBase collection every 90s (`POLL_INTERVAL_MS`), shows an unread dot, and links to `/discoveries`. It uses CSS classes (`.discovery-headline`, `.discovery-headline-label`, `.discovery-headline-text`) that are defined inline in `layout.tsx`'s `&lt;style dangerouslySetInnerHTML&gt;` block (lines 100‑140) but **the component is not currently imported/rendered anywhere in layout.tsx** — it appears to be a built-but-unwired component (or wired elsewhere, e.g. `BreadcrumbsNav`). Worth checking `breadcrumbs-nav.tsx` for usage. ### 2. `src/lib/cms.ts` (note: not `cms-content.ts` — the actual file is `src/lib/cms.ts`) Path: `/Users/scroobz/Navigation/saily/web/src/lib/cms.ts` Exported functions: - `listPublishedArticles(): Promise&lt;CmsArticle[]&gt;` — fetches `cms_articles` collection filtered `status = "published"`, sorted `-published_at` (i.e. newest first) — index `[0]` is effectively "latest". - `getPublishedArticle(slug: string): Promise&lt;CmsArticle | null&gt;` — single article by slug. `CmsArticle` shape (lines 3‑16): ```ts type CmsArticle = { id: string; slug: string; title: string; status: string; summary: string; body: string; tags: string[] | null; sources: string[] | null; citizen_science_links: string[] | null; hero_image: string; published_at: string; updated_at: string; }; ``` There is no dedicated `getLatestArticle()` helper — callers currently do `listPublishedArticles()` and take the first item (already sorted newest-first). For the ticker, STS-152 will likely want to add a `getLatestPublishedArticle()` convenience export (mirroring `getLatestDiscovery()` in `discoveries.ts`, see below) or just call `listPublishedArticles()` with `perPage=1`-style filtering added to the query (currently no `perPage` param is passed at all, so it fetches all published articles — worth optimizing for a ticker use). ### 3. "Discovery" / "milestone" data sources Primary existing concept: **`discoveries` PocketBase collection**, wrapped by `/Users/scroobz/Navigation/saily/web/src/lib/discoveries.ts`: ```ts type Discovery = { id: string; game: string; kind: string; headline: string; summary: string; cta_href: string; occurred_at: string; payload: Record&lt;string, unknown&gt; | null; }; ``` - `listDiscoveries(): Promise&lt;Discovery[]&gt;` — all discoveries, sorted `-occurred_at`. - `getLatestDiscovery(): Promise&lt;Discovery | null&gt;` — single latest record (`sort=-occurred_at&amp;perPage=1`). **This is exactly the "latest discovery/milestone" fetcher STS-152 needs** — already built and ready to reuse. The doc comment explicitly says: "Generalized across minigames: any producer writing into `discoveries` shows up here with no frontend change" — meaning any game (Landnam exoplanet hunts, Mars, Gaia, Rubin comet catchers, InSight) writing a milestone into this collection will surface automatically. This is the cross-ecosystem discovery feed referenced in `discovery-headline.tsx`'s comment: "see systems/cross-game-discoveries-feed.md" (a systems doc reference not found as an actual file in this repo — may live in another repo or be aspirational/missing). Consumers of `discoveries`: - `/Users/scroobz/Navigation/saily/web/src/app/discoveries/page.tsx` — full discoveries list page. - `/Users/scroobz/Navigation/saily/web/src/app/archive/page.tsx` - `/Users/scroobz/Navigation/saily/web/src/components/discovery-headline.tsx` — nav-headline pattern (polls PocketBase directly client-side via `getBrowserSailyPocketBaseUrl()` rather than using the `discoveries.ts` lib, since it needs client-side fetch/localStorage). - `/Users/scroobz/Navigation/saily/web/src/components/landing/landing-data.ts`, `project-survey.tsx`, `landing-footer.tsx`, `landing-placeholder-sections.tsx` — landing page references. Other candidate "milestone" sources found: - **Leaderboard / `user_stats`**: `/Users/scroobz/Navigation/saily/web/src/app/leaderboard/page.tsx` queries a `user_stats` table/view (`total_score, current_streak, wins, profiles(username)`) via `pocketbase.from("user_stats")` — this is player-level ranking data, not really "ecosystem discovery" content, so less suited to a ticker than `discoveries`. - No dedicated "classification count" aggregator lib was found (no `classifications.ts`, no leaderboard-events lib). The `discoveries` collection is the sanctioned single source of truth for milestone-style content. **Conclusion for STS-152**: combine `listPublishedArticles()`/latest article from `cms.ts` with `getLatestDiscovery()` from `discoveries.ts` — both are server-fetchable (no client auth needed, both collections are publicly readable per their PocketBase list/view rules) and both already return sorted-newest-first data. ### 4. `src/app/games/today/page.tsx` — page structure convention ```tsx import { Suspense } from "react"; import MissionFlowPage from "@/components/pages/games/mission-flow-page"; export default function Page() { return ( &lt;Suspense fallback={&lt;p className="muted" style={{ padding: "1rem" }}&gt;Loading mission…&lt;/p&gt;}&gt; &lt;MissionFlowPage /&gt; &lt;/Suspense&gt; ); } ``` Same pattern for `mars/page.tsx` (imports `MarsGamePage` from `@/components/pages/games/mars-game-page`). These subroutes are thin wrappers deferring all real logic/markup to a `@/components/pages/games/*` component wrapped in `Suspense` — no `metadata` export, no layout wrapper (the shared `dt-page-shell`/`BreadcrumbsNav` wrapper comes for free from root `layout.tsx`). This pattern is **not** the right model for a static "vision" overview page — it's built for interactive game/mission pages needing client-side data (`useSearchParams` etc., hence `Suspense`). The better model for `/games` (informational/prose) is the **`about/page.tsx`** and **`discoveries/page.tsx`** patterns (see #6 below): a plain server component exporting `metadata`, wrapped in `&lt;main className="panel" style={{ maxWidth: ..., margin: "2rem auto" }}&gt;`, using `.eyebrow`/`.muted` utility classes. ### 5. `src/components/landing/landing-data.ts` — `games` array shape Full file read at `/Users/scroobz/Navigation/saily/web/src/components/landing/landing-data.ts`. Relevant `games` export (lines 28‑50): ```ts export const games = [ { title: "Landnam", status: "Flagship", body: "Hunt for real exoplanets, then manage the ship and crew that turns a signal into a journey.", href: "https://starsailors.space", image: "/puzzles/puzzle-1.svg", }, { title: "Coral Clicker", status: "Coming Soon", body: "Identify real reef species and replicate compositions in this relaxed puzzle game.", href: "#", image: "/puzzles/puzzle-2.svg", }, { title: "Asteroid Hunters", status: "Planned", body: "Sweep survey frames for moving points of light and help track the rocks crossing our sky.", href: "#", image: "/puzzles/puzzle-3.svg", }, ]; ``` Fields per game: `title`, `status` (free-text: "Flagship" | "Coming Soon" | "Planned"), `body`, `href` (external URL or `"#"` placeholder), `image` (SVG path under `/puzzles/`). Consumed by `/Users/scroobz/Navigation/saily/web/src/components/landing/games-section.tsx` which renders a card grid (`tx-game-card`), using `&lt;Image&gt;` + `Kicker` (for status) + title/body. Note: this `games` array (3 entries: Landnam/Coral Clicker/Asteroid Hunters) is a **different, higher-level "Star Sailors ecosystem games" list** than the site's own `/games/{today,mars,gaia-variables,rubin-comet-catchers,insight}` subroutes (which are Daily-Transit-specific minigames within the Landnam/Star Sailors umbrella). STS-149's new `/games` overview page should clarify which list it's showing — likely the in-app minigames (today/mars/gaia/rubin/insight), not the `landing-data.ts` ecosystem-level list, though it could reuse both concepts (vision copy referencing the ecosystem, then a grid of the actual playable Daily Transit minigames). Worth flagging as a design decision in planning. The file also has `puzzleOptions`, `storyOptions`, `returnOptions` (survey option arrays, unrelated), `newsroom` (landing news cards), and `teamCards` (about/team info) — none directly relevant to `/games` vision copy. ### 6. Existing "vision"/manifesto-style prose page pattern `/Users/scroobz/Navigation/saily/web/src/app/about/page.tsx` is the clearest precedent: ```tsx export const metadata = { title: "About — The Daily Transit" }; export default function AboutPage() { return ( &lt;main className="panel" style={{ maxWidth: "640px", margin: "2rem auto" }}&gt; &lt;p className="eyebrow"&gt;About&lt;/p&gt; &lt;h1&gt;The Daily Transit&lt;/h1&gt; &lt;p className="muted"&gt;...&lt;/p&gt; &lt;p className="muted"&gt;...&lt;/p&gt; &lt;p className="muted"&gt;...&lt;/p&gt; &lt;/main&gt; ); } ``` Pattern: `metadata` export with `"&lt;Title&gt; — The Daily Transit"`; single `&lt;main className="panel"&gt;` panel box; `&lt;p className="eyebrow"&gt;` section label; `&lt;h1&gt;` title; sequence of `&lt;p className="muted"&gt;` paragraphs for narrative copy; inline external links via plain `&lt;a target="_blank" rel="noreferrer"&gt;`. `/Users/scroobz/Navigation/saily/web/src/app/discoveries/page.tsx` shows the richer variant: same `eyebrow`/`h1`/`muted` intro block, then a data-driven grid of `.panel` cards below the prose intro, plus a "Back to Home" button (`&lt;Link href="/" className="button"&gt;`). This is a strong template for `/games`: vision-copy intro (eyebrow/h1/muted paragraphs about streaks, citizen-science minigames, text interfaces, upvote-to-graduate concept) followed by a card grid (reusing/adapting `games-section.tsx`'s `tx-game-card` pattern or the `.panel` grid pattern) listing the current subroutes (`/games/today`, `/games/mars`, `/games/gaia-variables`, `/games/rubin-comet-catchers`, `/games/insight`). ### 7. Craft Saily Sprint 8 plan doc **Not found.** I searched: - `/Users/scroobz/Navigation/saily/.knowns/` and `/Users/scroobz/Navigation/saily/web/.knowns/` for "sprint 8", "games vision", "breaking news", "ticker", "streak", "graduate", "upvote", "text-based interface" — no matching doc with that combined wording. - `/Users/scroobz/Navigation/workspace/projects/saily/` and `/Users/scroobz/Navigation/workspace/projects/tdt/` ticket/doc trees — no "Craft Saily Sprint 8" file, no ticket referencing streaks+minigames+text-interfaces+upvote-graduation together. - Repo-wide grep for "sprint 8"/"Craft Saily Sprint" across `~/Navigation` (excluding node_modules/.venv/.git) — only unrelated hits (a `bokeh` sample-data file, git branch refs named `mvp-sprint`). - The closest tangential hits: `saily-product-spec.md` (both the canonical copy at `saily/.knowns/docs/saily-specs/saily-product-spec.md` and the organized copy) mentions **streaks** generically (§4.3 "Streaks &amp; XP" — "Streak: Daily consecutive play count", "Repair Streaks... consume a Data Chip", "+1xp for every 10 days") but nothing about citizen-science minigames-as-a-vision-statement, text-based interfaces, or community-upvote-to-graduate mechanics, nor about a breaking-news ticker. **Conclusion**: the "Craft Saily Sprint 8" plan doc referenced in the ticket prompt does not exist in this repo, the parent `saily` repo's `.knowns`, or the `workspace/projects/saily` or `tdt` ticket trees. It's possible it lives in an external tool (e.g. the `craft_saily` doc referenced via `mcp__claude_ai_Craft__craft_read`) rather than in the filesystem — recommend checking Craft directly (there's a `claude_ai_Craft` MCP tool available: `craft_read`) since a filesystem search turned up nothing. I did not use that tool since it's outside my read-only filesystem-search scope, but flagging it as the likely actual source of the sprint plan wording.</result> <usage><subagent_tokens>60438</subagent_tokens><tool_uses>28</tool_uses><duration_ms>122255</duration_ms></usage> </task-notification>.
