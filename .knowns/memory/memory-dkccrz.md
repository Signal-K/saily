---
id: dkccrz
title: Project workflow decision
layer: project
category: decision
status: proposed
tags:
  - project
  - workflow
createdAt: '2026-07-06T09:20:47.262Z'
updatedAt: '2026-07-06T09:20:47.262Z'
---

<task-notification> <task-id>a1e3ff499009617de</task-id> <tool-use-id>toolu_01R7U4VJWGFbeRnPVNCx9CZz</tool-use-id> <output-file>/private/tmp/claude-501/-Users-scroobz-Navigation-saily/451ff047-5711-4b28-9d32-5a6ae4652f8c/tasks/a1e3ff499009617de.output</output-file> <status>completed</status> <summary>Agent "Audit mobile responsiveness issues" finished</summary> <note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note> <result>## Mobile/Responsiveness Audit — The Daily Sail **Styling approach:** Mixed, not uniform. Tailwind v4 is installed (`@tailwindcss/postcss`, `@theme` block in `globals.css`) and is used properly with responsive prefixes (`sm:`, `md:`, `lg:`, `max-sm:`) in newer code like `web/src/components/pages/home-page.tsx`. But a large portion of the app — `globals.css` component layer, `daily-transit-masthead.tsx`, `landing-styles.tsx`, and the inline `&lt;style jsx global&gt;` block in `layout.tsx` — is hand-written plain CSS with `grid-template-columns`/flex layouts and its own ad-hoc `@media (max-width: …)` breakpoints (900px, 760px/767px, 640px, 520px, 380px — no single shared breakpoint scale). Any responsive work should reuse this existing max-width convention (and Tailwind's default `sm/md/lg` where Tailwind utility classes are already used) rather than introducing a new system. **Important context finding:** Several files that look like primary UI are actually dead code not reachable from any route — worth confirming with the team before "fixing" them: - `web/src/components/header-search.tsx` and `web/src/components/header-calendar.tsx` are not imported anywhere. - The elaborate `.dt-nav`/`.dt-mega`/`.dt-ecosystem`/`.header-search-*` CSS in `globals.css:405-696` styles markup that no component renders — the live header is `DailyTransitMasthead` (`web/src/components/daily-transit-masthead.tsx`), which only renders a logo + theme toggle (no nav links, no search, no calendar) — so the app currently has **no primary navigation UI at all** on pages under `layout.tsx` except breadcrumbs. - `landing/games-section.tsx`, `citizen-science-section.tsx`, `team-section.tsx`, `newsroom-section.tsx` are not imported by `landing-page.tsx` (only `DailyLiveSection` + `ReaderBriefingSection` + masthead/footer are live). - `.mobile-bottom-nav`/`.mobile-tab` classes exist in `globals.css:1696-1709` and `globals.css:2666-2689` but no component renders that markup — a mobile tab bar was designed but never wired up. **Top issues, ordered by mobile impact:** - `web/src/components/daily-transit-masthead.tsx:1-132` (the actual site header) — has no primary navigation links at all on mobile or desktop; a user can't get to Games/Calendar/Discuss/Search from the header. This is the single biggest UX gap, not really a "doesn't stack" bug but an absence of nav. - `globals.css:1789-1794` `.puzzle-workspace` (game puzzle layout: canvas + sidebar) is `grid-template-columns: minmax(0,1fr) minmax(280px,360px)` and only collapses to 1 column at `900px` (`globals.css:2596-2598`) — fine, but the sidebar's `min-width: 280px` combined with the light-curve chart forcing `min-width: 720px` (`globals.css:1812-1818`, wrapped in `.puzzle-lightcurve-wrap { overflow-x: auto }`) means the chart itself always needs horizontal scrolling below ~720px — confirm this is acceptable UX vs. a truly responsive SVG. - `web/src/components/landing/landing-masthead.tsx:21-34` `.tx-public-nav-primary` (5 links: Today/Archive/Articles/Discoveries/Play) has no `overflow-x`/wrap handling of its own; only `.tx-public-nav-secondary` scrolls. It's covered by a `@media (max-width: 860px)` rule in `landing-styles.tsx:2507-2511` that forces `white-space: nowrap` + `overflow-x` on the row — but between full-width and 860px there's no wrap, so on iPad-ish widths (768–860px) it's already relying on scroll, and `tx-public-title-row` gets `padding-right: 9rem` (`landing-styles.tsx:2498`) to avoid colliding with the absolutely-positioned status pill (`landing-styles.tsx:206-211`) — a fairly fragile magic-number fix. - `globals.css:1266-1275` `.dt-masthead-top` (dead code, see above) is a 4-column grid (`minmax(190px,auto) minmax(220px,1fr) auto auto`) meant for brand+search+nav+actions, with breakpoints at 900/640 — this duplicated/legacy CSS should probably be deleted rather than fixed, since the live masthead component (`daily-transit-masthead.tsx`) reimplements a simpler 2–3 column version inline. - `globals.css:1237-1247` `.tx-article-grid` (`repeat(3, minmax(0,1fr))`) and `globals.css:2469-2473` `.gallery-grid` (`repeat(3, minmax(0,1fr))`) both only collapse to 1 column at the 900px breakpoint (`globals.css:2600-2605`) — no intermediate 2-column step for tablet widths. - `layout.tsx:230-246` inline `&lt;style&gt;`: `.calendar-page-header`/`.search-page-header`/`.calendar-page-toolbar` use `display:flex; justify-content:space-between` and only get `flex-direction: column` at `760px` (`layout.tsx:366-377`) — content like month label + nav buttons could feel cramped between ~480–760px since there's no intermediate step, and `.calendar-page-grid` cells (`layout.tsx:284-289`, 7-column grid, `min-height: 72px`) only drop to `min-height: 48px` at 760px (`layout.tsx:379-381`) with no reduction in horizontal gap/padding for very narrow phones (&lt;360px). - `landing-styles.tsx:341-345` `.tx-live-grid` (main "Daily Live" briefing: `minmax(0,1.45fr) minmax(280px,0.75fr)`) and `:1409-1413` `.tx-ed-stats` (`repeat(3, 1fr)` stat row) — the live-grid does collapse to 1fr at 860px (`landing-styles.tsx:2513-2518`), but `.tx-ed-stats` has no responsive override anywhere in the file, so on very narrow screens (&lt;360px) three stat numbers get squeezed. - `landing-styles.tsx:1135` `.tx-rank-item` (ranking sheet row) is a 5-column grid (`1.75rem 2rem 1fr auto auto`) that only simplifies to 4 columns at 520px (`landing-styles.tsx:2597`) — reasonably handled already but worth checking touch target sizes below 380px. - Small tap targets: `.profile-trigger`/`.profile-skeleton` (`globals.css:581-601`) are `2.25rem` (36px) square — below the commonly recommended 44px minimum touch target; `.dt-nav-link` padding shrinks to `0.7rem` inline / `0.62rem` font at 640px (`globals.css:688-691`) which is fine for text but worth double-checking actual link hit-area height. - Hover-only affordances: `.dt-mega` mega-menus and `.dt-nav-item:hover .dt-mega` (`globals.css:472-491`) rely purely on `:hover`/`:focus-within`, no click/tap toggle — moot right now since this markup isn't rendered, but if it's revived for a real nav it will need a tap-friendly trigger for mobile. **Existing responsive conventions worth reusing (don't invent new ones):** - Plain-CSS breakpoints already standardized loosely around `900px` (tablet), `760/767px`, `640px` (phone), `520px`, and `380px` (very narrow) — reuse these exact values for consistency rather than Tailwind's default `640/768/1024`. - `clamp(...)` is used extensively for fluid type sizing (e.g. `--type-h1`, `.dt-brand-name`, `.tx-cos-title`) — good pattern to keep using for headings instead of fixed `font-size` + breakpoint overrides. - `width: min(var(--spacing-content-max), calc(100% - Xrem))` is the standard container-width pattern (`.container`, `.dt-page-shell`, `.tx-page`) — reuse this rather than fixed `max-width` + separate mobile padding rules. - Tailwind responsive utilities (`sm:`, `md:`, `lg:`, `max-sm:`) are already the pattern in `home-page.tsx` — new/rewritten components should prefer Tailwind classes over new `&lt;style jsx&gt;` blocks where feasible, for consistency with the newest code in the repo. - `overflow-x: auto` + `scrollbar-width: none`/hidden `::-webkit-scrollbar` is the established pattern for horizontally-scrolling nav/ribbons on mobile (`tx-public-nav-secondary`, `tx-update-ribbon`, `.dt-nav`) — reuse instead of wrapping.</result> <usage><subagent_tokens>124321</subagent_tokens><tool_uses>25</tool_uses><duration_ms>151345</duration_ms></usage> </task-notification>.
